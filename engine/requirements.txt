numpy>=1.26
pillow>=10.0
tqdm>=4.66
rich>=13.7
click>=8.1
open-clip-torch>=2.24.0
torch>=2.2

# API server
fastapi>=0.110
uvicorn[standard]>=0.27

# macOS folder picker uses AppKit via pyobjc-framework-Cocoa

# macOS OCR (Apple Vision)
pyobjc-core>=10.0; platform_system == "Darwin"
pyobjc-framework-Vision>=10.0; platform_system == "Darwin"
